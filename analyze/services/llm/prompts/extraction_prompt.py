EXTRACTION_PROMPT = """
Извлеки весь текст с изображения.

ВАЖНО:
- сохрани структуру
- ничего не пропускай
- не сокращай
- исправь очевидные OCR ошибки
- сохрани таблицы и списки
- сохрани порядок текста

Верни только текст.
"""

MATH_EXTRACTION_PROMPT = """
Извлеки весь текст с изображения контрольной или учебного задания.

КРИТИЧЕСКИ ВАЖНО ДЛЯ ФОРМУЛ:
- все математические выражения, уравнения, степени, индексы, дроби, корни и скобочные выражения верни в LaTeX;
- каждую формулу оборачивай в одинарные долларовые разделители: $...$;
- не используй $$...$$;
- не оставляй формулы плоским текстом, если видны степени, дроби, корни или специальные математические знаки;
- сохраняй буквы подпунктов: а), б), в), г);
- не решай задания и не добавляй ответы;
- сохрани порядок строк, нумерацию, списки и структуру исходного документа;
- если часть формулы не уверена, передай ее максимально близко в LaTeX, не пропускай.

Примеры формата:
- видишь x² - 5x = 0 -> верни $x^{2}-5x=0$
- видишь 3xy·(4yz) -> верни $3xy \\cdot (4yz)$
- видишь дробь с числителем x+1 и знаменателем 2 -> верни $\\frac{x+1}{2}$

Верни только распознанный текст.
"""
